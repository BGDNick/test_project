import os

import numpy as np
import pandas as pd

pd.DataFrame
np.array
os.path.isabs


def test(
    a: int,
    b: float,
    c: str,
    d: pd.DataFrame,
) -> None:
    """_summary_

    Args:
        a (int): _description_
        b (float): _description_
        c (str): _description_
        d (pd.DataFrame): _description_
    """
    ...
